//structure = var, then defined func, then overall func
/*
divApp.appendChild( newrow(),
                    newrow(
                        newcol(
                            newrow(
                                newcol(), newcol(),newcol()
                            newrow(
                                newcol(), newcol(),newcol()
                            newrow(
                                newcol(), newcol(),newcol()
                            )
                        newcol(
                            newrow(victory text)
                            newrow(turn text)
                            newrow(score text)
                            newrow(
                                newbtn(reset)
                                newbtn(start)
                                newbtn(rules)
                            )
                        )
                    newrow()
                    */
